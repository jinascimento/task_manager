<%= form_for @task, remote: true do |f| %>
  <div class="modal-body">
        <div class="form-group">
          <%= f.label 'Título' %>
          <%= f.text_field :title, class: 'form-control form-control-sm', required: true %>
        </div>
        <div class="form-group">
          <%= f.label 'Período' %>
          <%= f.text_field :date_range, class: "form-control input-sm  date-range-picker" %>
        </div>
        <div class="form-group">
          <%= f.hidden_field :start, value: Date.today, class: 'form-control input-sm start_hidden' %>
          <%= f.hidden_field :end, value: Date.today, class: 'form-control input-sm end_hidden' %>
          <%= f.label 'Cor' %>
          <%= f.select(:color, [['Preto','black'], ['Verde','green'], ['Vermelho','red']], {}, {class: 'form-control'}) %>
        </div>
  </div>
  <div class="modal-footer">
    <div class="form-actions">
      <%= f.button :submit, class: 'btn btn-primary' %>
      <%= link_to 'Excluir',
                  task,
                  method: :delete,
                  class: 'btn btn-danger',
                  data: { confirm: 'Are you sure?' },
                  remote: true unless @task.new_record? %>
    </div>
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Fechar</button>
  </div>
  <% end %>
<%#= javascript_include_tag 'plugins-init/form-pickers-init.js' %>
<%#= javascript_include_tag 'date_range_picker' %>