toastr.success('Sua tarefa foi agendada.', 'Boa!')

$('.calendar').fullCalendar(
    'renderEvent',
    $.parseJSON("<%=j render(@task, format: :json).html_safe %>"),
    true
);
$('.modal').modal('hide');