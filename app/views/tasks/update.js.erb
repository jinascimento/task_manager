$('.calendar').fullCalendar('removeTask', [<%= @task.id %>]);
$('.calendar').fullCalendar(
    'renderTask',
    $.parseJSON("<%=j render(@task, format: :json).html_safe %>"),
    true
);
$('.modal').modal('hide');